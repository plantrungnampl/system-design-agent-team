mappings:
  - source: ORDER_ID
    target: orders.id
    transform: integer
  - source: CUSTOMER_ID
    target: orders.customer_id
    transform: trim
  - source: TOTAL_CENTS
    target: orders.total_cents
    transform: integer
